import 'package:flutter/material.dart';
import 'package:provider/provider.dart';
import 'dart:math';


void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return ChangeNotifierProvider(
      create: (context) => MyAppState(),
      child: MaterialApp(
        title: 'Namer App',
        theme: ThemeData(
          colorScheme: ColorScheme.fromSeed(seedColor: const Color.fromARGB(255, 245, 186, 8)),
        ),
        home: MyHomePage(),
      ),
    );
  }
}

/*==============================
================================
================================
================================
================================
*/

class MyAppState extends ChangeNotifier {

  var random = Random();

  var leftButtonLabel = "";  //  label of the left button
  var middleButtonLabel = ""; //  label of the middle button
  var rightButtonLabel = "";  //  label of the right button

  
  var equation = "";  //  linked to equation text box 
  var resultMessage = ""; // linked to result message text box
  
  
  var correctAnswer = 0;
  
  
  
  // the three buttons will call this function on pressed
  
  void answerLeftButton(){

    notifyListeners();
  }

  void answerMiddleButton(){

    notifyListeners();
  }

  void answerRightButton(){

    notifyListeners();
  }


  // linked with Next button
  void next(){


    notifyListeners();
  }


}

/*==============================
================================
================================
================================
================================
*/

// ...

class MyHomePage extends StatefulWidget {
  @override
  State<MyHomePage> createState() => _MyHomePageState();
}

class _MyHomePageState extends State<MyHomePage> {
  
    var selectedIndex = 0; // create variable
  @override
  Widget build(BuildContext context) {

    var page;

    if (selectedIndex == 0){
      page = HomePage();
    }
    else if (selectedIndex == 1){
      page = ListPage();
    }

    return Scaffold(
      body: Row(
        children: [
          SafeArea(
            child: NavigationRail   (
              extended: false,
              destinations: [
                NavigationRailDestination(
                  icon: Icon(Icons.home),
                  label: Text('Home'),
                ),
                NavigationRailDestination(
                  icon: Icon(Icons.list),
                  label: Text('Favorites'),
                ),
              
              ],
              selectedIndex: selectedIndex,
              onDestinationSelected: (value) {
                setState(() {
                  selectedIndex = value;
                });;
              },
            ),
          ),
          Expanded(
            child: Container(
              color: Theme.of(context).colorScheme.primaryContainer,
              child: page,
            ),
          ),
        ],
      ),
    );
  }
}


// page one
class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) {
    var appState = context.watch<MyAppState>();
    

    return Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Row(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
          Container(child: InputDecorator(decoration: InputDecoration(labelText: 'equation',border: OutlineInputBorder(),),child: Text(appState.equation),), width: 200,),
              

            ],
          ),
          SizedBox(height: 50),

          Row(
        mainAxisAlignment: MainAxisAlignment.center,

            children: [
              ElevatedButton(onPressed: appState.answerLeftButton, child: Text(appState.leftButtonLabel)),
              SizedBox(width: 20,),
              ElevatedButton(onPressed: appState.answerMiddleButton, child: Text(appState.middleButtonLabel)),
              SizedBox(width: 20,),
              ElevatedButton(onPressed: appState.answerRightButton, child: Text(appState.rightButtonLabel)),
            ],
          ),
          SizedBox(height: 100),
          Container(child: InputDecorator(decoration: InputDecoration(labelText: 'resultMessage',border: OutlineInputBorder(),),child: Text(appState.resultMessage),), width: 200,),
          
          SizedBox(height: 100),
          ElevatedButton(onPressed: appState.next, child: Text("Next")),

          Row(
            mainAxisSize: MainAxisSize.min,
            children: [
              SizedBox(width: 10), 
            ],
          ),
        ],
      ),
    );
  }
}



// page two

class ListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    var appState = context.watch<MyAppState>();


    return               InputDecorator(
              decoration: InputDecoration(
                labelText: 'XP',
                border: OutlineInputBorder(),
              ),
              child: Text('Content inside'),
            );
  }
}

